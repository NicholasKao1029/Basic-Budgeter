package Interfaces;

public interface Income {

    //REQUIRES: nothing
    //MODIFIES: this
    //EFFECTS: takes in an amount of expense and returns the surplus or
    public int surplus(int expense);
}
